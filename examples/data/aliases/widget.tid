title: $:/plugins/rimir/namespace/examples/data/aliases/widget
aliases: Widget [[The Widget]] OldName

Demo target for the field-aliases example. Three alias tokens declared
in the `aliases` field: `Widget`, `The Widget`, and `OldName`. Each
resolves to this tiddler from anywhere when the //Field aliases// feature
flag is on.
